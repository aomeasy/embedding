name: Run Embedding

on:
  workflow_dispatch:
  schedule:
    - cron: '0 1 * * *'  # รันทุกวันตอนตี 1 UTC (8 โมงเช้าไทย)

jobs:
  embed:
    runs-on: ubuntu-latest
    environment: TIDB_URL   # <<< สำคัญ! ชื่อ Environment ต้องตรงกับที่คุณตั้ง

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run embedding script
        env:
          TIDB_URL: ${{ secrets.TIDB_URL }}
          EMBEDDING_API_URL: ${{ secrets.EMBEDDING_API_URL }}
          EMBEDDING_MODEL: ${{ secrets.EMBEDDING_MODEL }}
        run: |
          echo "🔑 Connecting to TIDB"
          python main.py
